\begin{figure*}[t]
\centering
\includegraphics[width=0.74\textwidth]{figures/fig2_dissociation.pdf}
\caption{\textbf{The same method, the same layers, two different verdicts.} Quality against
the fraction of blocks skipped for Qwen2.5-0.5B (circles), 1.5B (squares) and 7B
(triangles), layers chosen by measured residual damage, the deployable rule, with
every predictor skipping the identical blocks. \emph{Left:} on WikiText-2 likelihood
\mname (solid) sits below plain skipping (dashed) at every budget and all three scales,
and below the scalar predictor (dotted). \emph{Right:} on downstream accuracy the same
curves at the same layers are indistinguishable from plain skipping. Bars and the shaded
inset band are $\pm 1$ binomial standard error; \emph{no} deployable difference reaches
significance at $95\%$ at any budget or scale (\Cref{sec:app-noise}), so the right panel
is an absence of effect, not a regression. A repair measured in one currency, and its
absence in another. 0.5B in FP32 on an NVIDIA TITAN X (Pascal); 1.5B and 7B in BF16 on a
single NVIDIA RTX 3090.}
\label{fig:dissociation}
\end{figure*}
